# AI UI 灵感库 · 产品与技术方案整理

> 整理时间：2026-08-14 ｜ 对标参考：uinotes.com
> 线上地址：https://wanxudong2018-crypto.github.io/ai-ui-notes/

---

## 一、背景与定位

**起点问题**：要不要克隆 uinotes（移动端 UI 截图汇总站）？

**结论：不直接克隆，做差异化。**
- 直接克隆的风险：同质化严重、无技术/内容壁垒、容易被质疑抄袭、价值存疑。
- 我们的差异化方向：**聚焦「AI 助手 / Agent 类 App」的 UI 灵感库**，并且**按「设计问题」组织内容**——用户不是来漫无目的地逛截图，而是带着具体设计问题（如「首次引导怎么做」「空状态怎么设计」）来找参考。

---

## 二、四个差异化角度（已选定）

从候选里选出 4 个可做方向：

| # | 角度 | 作用 | 阶段 |
|---|------|------|------|
| 1 | **AI 助手 / Agent 垂直库** | 内容定位差异化，避开通用 UI 汇总红海 | MVP 核心 |
| 2 | **按设计问题组织** | 信息架构差异化，解决「搜不到参考」的痛点 | MVP 核心 |
| 3 | **AI 视觉搜索 + 组件自动标注** | 技术壁垒，别人难抄 | 阶段二 |
| 4 | **交互流录屏** | 增值能力，可做成付费层 | 阶段三 |

> MVP 只用 ① + ②；③ 用来建壁垒；④ 用来做商业化。

---

## 三、第一性原理 MVP 范围

> 核心原则：**只验证「有没有人愿意按设计问题来找 UI 参考」这一件事。**

MVP 只做三件事：
1. **采集截图**（人工，从真实 AI App 截）
2. **按问题标注**（在飞书表里打标签）
3. **按问题展示**（前端画廊按设计问题导航）

**明确砍掉（等验证后再说）**：
- 账号系统 / 用户上传（UGC）
- AI 自动标注（成本高、MVP 阶段人工更准）
- 版本演化对比（需长期积累，不做）
- 交互流录屏（放到阶段三）
- 付费 / 后端数据库（MVP 零后端）

**成本现实**：主要内容成本在「采集 + 标注约 200 张真实 AI App 截图」，代码只占约 10%。

---

## 四、技术架构（已落地）

```
飞书多维表格 (CMS)
      │  sync_feishu.py 拉取
      ▼
  data.js  (window.APP_DATA)
      │  纯静态前端读取
      ▼
  纯静态站点 (index.html + app.js + styles.css)
      │  GitHub Actions 每小时 / push / 手动触发
      ▼
  GitHub Pages  (线上站点)
```

| 层 | 技术选型 | 说明 |
|----|----------|------|
| 前端 | 纯静态 HTML/CSS/JS | 无后端、无 fetch，靠 `data.js` 渲染，极便宜极稳 |
| 内容管理 | 飞书多维表格 | 零成本 CMS；用户用得惯的编辑器；改表即改内容 |
| 同步脚本 | `sync_feishu.py` | 飞书 API 拉表 → 生成 `data.js`（含列名空格容错、空行过滤）|
| 部署 | GitHub Pages + Actions | 每小时自动同步飞书、push 即部署、可手动 Run workflow |
| 图片 | 本地 `images/` 相对路径 | 随仓库一起部署，无需图床；压 WebP 后几千张无压力 |
| 备选部署 | `deploy-aliyun.yml` | 流量起来后切阿里云 OSS+CDN（备案域名已备）|

**关键文件**：
- `index.html` / `styles.css` / `app.js`：画廊前端（侧边栏按设计问题导航 + 9:16 缩略图网格 + 竖图优先弹窗含步骤切换）
- `data.js`：由脚本生成，驱动整个站点
- `sync_feishu.py`：飞书 → 前端 的同步链
- `.github/workflows/deploy.yml`：GitHub Pages 自动部署
- `.github/workflows/deploy-aliyun.yml`：阿里云备选部署
- `config.json`（已 gitignore，防凭据泄露）/ `config.example.json`：飞书凭据与列映射

---

## 五、数据模型

**一条飞书记录 = 一个 App 的截图流程**

```js
{
  problem: "首次引导",        // 设计问题（侧边栏按此导航）
  app: "豆包",                // App 名称
  platform: "iOS",           // 平台
  images: [                  // 多张截图（按顺序排列 = 一个交互流）
    "images/001.png",
    "images/002.png",
    "images/003.png"
  ],
  steps: [                   // 每步说明（与 images 一一对应）
    "1、点 XX",
    "2、点 XX",
    "3、点 XX"
  ],
  note: ""                   // 备注
}
```

- 单屏截图 = `images` 只有 1 张、`steps` 只有 1 条的退化情况。
- 飞书字段对应：设计问题 / App 名称 / 平台 / 图片 URLs（多行，每行一个）/ 步骤说明（多行）/ 备注。

---

## 六、当前进度（截至 2026-08-14）

| 项 | 状态 |
|----|------|
| 站点上线 | ✅ https://wanxudong2018-crypto.github.io/ai-ui-notes/ |
| 飞书 → 站点自动同步 | ✅ 跑通（每小时 cron + 手动触发）|
| 图片加载 | ✅ `images/001.png` 实测 200 / 667KB |
| 示例数据 | ⏳ 飞书里那条「豆包」步骤说明仍是占位文字，待填真实内容 |
| 前端视觉 | ⏳ 用户认为偏乱，待优化 |
| 图片压缩 | ⏳ 未接自动压缩（原图 ~0.6MB/张，可接受但未优化）|

---

## 七、路线图

- **阶段一（当前）**：静态站 + 飞书 CMS，验证「按设计问题找参考」的需求是否成立。
- **阶段二**：接入 AI 视觉搜索 + 组件自动标注，建立技术壁垒（别人难抄）。
- **阶段三**：交互流录屏能力，作为付费增值层探索商业化（参考站年订阅约 70 元）。
- **未来扩容**：流量起来后从 GitHub Pages 切阿里云 OSS+CDN，直接绑已备案自有域名。

---

## 八、成本与容量边界

- **GitHub Pages 零成本**。图片压成 WebP（~150KB/张）后，约 **6,600 张** 仍在推荐 1GB 上限内；未压 PNG（~0.6MB）约 1,700 张。
- **单张限制**：> 100MB 会被拒绝推送（手机截图远小于此）。
- **结论**：第一阶段 200 张毫无压力；想做几千张大库，压一下即可，必要时再上 OSS。

---

## 九、待办清单（接下去做）

1. **前端优化**（用户计划）：整体布局 / 卡片网格 / 弹窗 / 视觉风格，先审一遍再动手。
2. **飞书填真实案例**：把示例占位文字换成真实 App 截图流程。
3. **接入自动压缩**：部署流程加一步，原图自动转 WebP 再上线，用户免手动压。
4. **安全收尾**：聊天里曾泄露飞书 App Secret，去飞书后台重置并更新 GitHub Secret。
5. **（可选）图片懒加载**：滚动到才加载，进一步加快首屏。
6. **（未来）绑自有域名**：GitHub Pages Custom domain 或切阿里云 OSS+CDN。
